pragma solidity 0.6.6;


// import "../token/SecurityTokenPartition.sol";

contract SecurityTokenPartitionFactory {
	/*
    // TODO
    //  we could use this to make sure, even admins cannot add partitions that are a SecurityTokenPartition.sol

    function createNew(address securityTokenAddress, bytes32 partition) public returns(SecurityTokenPartition) {
        return new SecurityTokenPartition(securityTokenAddress, partition);
    }
    */
}
